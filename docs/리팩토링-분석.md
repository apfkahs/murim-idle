# 소스코드 토큰 효율 분석 (2026-04-12)

코드 분할/리팩토링 시 참고용. 실제 리팩토링은 별도 작업으로 진행.

---

## 1. gameLoop.ts (1,345줄) — 분할 강력 추천

| 섹션 | 줄 수 | 내용 |
|------|-------|------|
| 헬퍼 함수 | ~90줄 | `applyVariance`, `calcAttackDamage`, `calcEnemyDamage` 등 |
| 비전투 로직 | ~30줄 | 기운 생산, HP 회복 |
| 플레이어 공격 | ~250줄 | 스킬 선택, 데미지 계산, 절초 처리 |
| 적 처치/보상 | ~260줄 | 드롭, 재료, 필드 해금, 탐험/수련 완료 |
| 적 공격 | ~320줄 | 적 타이머, 패시브(회피/흡수), 보스 패턴 |
| 업적/튜토리얼 | ~30줄 | 업적 체크 |

**추천 분할:**
- `damageCalc.ts` — `applyVariance`, `calcAttackDamage`, `calcEnemyDamage` (~30줄, 순수 함수)
- `playerCombat.ts` — 플레이어 공격 페이즈 (~250줄)
- `enemyCombat.ts` — 적 공격/보스 패턴 (~320줄)
- `battleRewards.ts` — 적 처치 후 보상 처리 (~260줄)
- `gameLoop.ts` — 오케스트레이터 (~200줄)

**토큰 효과:** 개별 서브시스템 작업 시 70-80% 절감

---

## 2. index.css (1,515줄) — 분할 추천

| 섹션 | 줄 수 |
|------|-------|
| 변수/리셋 | ~50줄 |
| 레이아웃 | ~90줄 |
| 공통 컴포넌트 | ~220줄 |
| 전투 UI | ~170줄 |
| 필드/몬스터 | ~350줄 |
| 인벤토리 | ~70줄 |
| 장비 | ~90줄 |
| 심득/무공 | ~95줄 |

**추천 분할:** `variables.css`, `layout.css`, `components.css`, `battle.css`, `field.css`, `inventory.css`, `equipment.css`, `mastery.css`

**추가 개선:** `rgba(255,215,0,0.X)` 40회 반복 -> CSS 변수화 (`--gold-t-10`, `--gold-t-20` 등)

**토큰 효과:** 도메인별 작업 시 60-65% 절감

---

## 3. ArtsTab.tsx (722줄) — 서브컴포넌트 추출 추천

| 섹션 | 줄 수 |
|------|-------|
| 메인 컴포넌트 | ~420줄 |
| MasteryPanel (내부 함수) | ~280줄 |

**추천 추출:**
- `components/arts/MasteryPanel.tsx` — 이미 내부 함수로 분리됨, 파일로 독립 (~280줄)
- `components/arts/ArtCard.tsx` — 동적 무공 카드 렌더링 (~110줄)
- `components/arts/ProficiencyCard.tsx` — 숙련도 표시 (~40줄)

**코드 품질 이슈:**
- `useGameStore.getState()` 사용 — React 라이프사이클 밖에서 접근, 스테일 데이터 위험
- `equippedProfTypes` 계산 — 매 렌더마다 재실행, `useMemo` 필요

**토큰 효과:** ArtsTab만 로드 시 50% 절감

---

## 4. monsters.ts (610줄), arts.ts (535줄) — JSON 분리 비추천

- TypeScript 인터페이스/타입과 헬퍼 함수 포함
- JSON 변환 시 타입 안전성 상실 + 런타임 검증 코드 추가 필요
- .claudeignore에 넣으면 content-writer 에이전트가 접근 불가
- **결론: TS 유지가 낫다**

---

## 5. store/slices/ — 중복 패턴

| 패턴 | 위치 | 중복 |
|------|------|------|
| `calcUsedPoints` 함수 | artsSlice, progressSlice | 완전 동일 15줄 x 2 |
| `battleMode !== 'none'` 가드 | 4개 슬라이스 | 16회 반복 |
| 배열 중복 제거 로직 | 5+ 곳 | 8회 반복 |

**추천:** `store/utils/sliceHelpers.ts` 생성
- `calcUsedPoints(state)` — 공유 함수 (30줄 중복 제거)
- `addToSet<T>(array, item)` — 배열 중복 제거 유틸 (8패턴 통합)

**총 절감:** 38-42줄 확정 + 78-110줄 (가드 추상화 시)
